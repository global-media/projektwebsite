        <div class="form-group">
            <label>Name</label>
            <%= f.text_field :name, class: 'form-control', placeholder: 'Name' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Publish Date</label>
            <%= f.date_field :publish_at, class: 'form-control' %>
        </div>        
        <div class="form-group">
            <label>Retract Date</label>
            <%= f.date_field :retract_at, class: 'form-control' %>
        </div>        
        <div class="form-group">
            <label>Image Upload</label>
            <%= f.file_field :image, as: :file, class: 'form-control' %>
        </div>        
        <% unless @category.new_record? || !@category.image.exists? %>
        <div class="form-group">
            <label>Image Tag</label>
            <%= image_tag @category.image.url(:thumb) %>
        </div>
        <div class="form-group">
            <label>Image Tag</label>
            <%= image_tag @category.image.url(:medium) %>
        </div>
        <div class="form-group">
            <label>Image Tag</label>
            <%= image_tag @category.image.url %>
        </div>
        <% end %>
