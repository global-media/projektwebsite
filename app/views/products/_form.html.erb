        <div class="form-group">
            <label>Name</label>
            <%= f.text_field :name, class: "form-control#{' error' unless @product.errors[:name].blank?}", placeholder: 'Name' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Metadata</label>
            <%= f.text_field :metadata, class: "form-control#{' error' unless @product.errors[:metadata].blank?}", placeholder: 'Metadata' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Description</label>
            <%= f.text_area :description, class: "form-control#{' error' unless @product.errors[:description].blank?}", placeholder: 'Description' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Collections</label>
            <%= f.select :collection_ids, collection_options, {selected: @product.collections.collect(&:id)}, {class: 'form-control', multiple: true} %>
        </div>
        <div class="form-group">
            <label>Base SKU</label>
            <%= f.text_field :base_sku, class: "form-control#{' error' unless @product.errors[:base_sku].blank?}", placeholder: 'Base SKU' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Base Price</label>
            <%= f.text_field :base_price, class: "form-control#{' error' unless @product.errors[:base_price].blank?}", placeholder: 'Base Price' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Sale Price</label>
            <%= f.text_field :sale_price, class: "form-control#{' error' unless @product.errors[:sale_price].blank?}", placeholder: 'Sale Price' %>
            <!--p class="help-block">Example block-level help text here.</p-->
        </div>
        <div class="form-group">
            <label>Publish Date</label>
            <%= f.date_field :publish_at, class: "form-control#{' error' unless @product.errors[:publish_at].blank?}" %>
        </div>        
        <div class="form-group">
            <label>Retract Date</label>
            <%= f.date_field :retract_at, class: "form-control#{' error' unless @product.errors[:retract_at].blank?}" %>
        </div>        
        <div class="form-group">
            <label>Main Image Upload</label>
            <%= f.file_field :main_image, as: :file, class: "form-control#{' error' unless @product.errors[:main_image].blank?}" %>
        </div>        
        <% unless @product.new_record? || !@product.main_image.exists? %>
        <div class="form-group">
            <label>Main Image</label>
            <%= image_tag @product.main_image.url(:thumb) %>
        </div>
        <div class="form-group">
            <label>Image</label>
            <%= image_tag @product.main_image.url(:medium) %>
        </div>
        <div class="form-group">
            <label>Image</label>
            <%= image_tag @product.main_image.url %>
        </div>
        <% end %>

