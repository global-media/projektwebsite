      <div class="form-group">
          <label>Status</label>
          <%= select_tag "order[status_id]", options_for_select(order_status_options, @order.status_id), {class: "form-control#{' error' unless @order.errors[:status_id].blank?}"} %>
      </div>
      <div class="form-group">
          <label>Subtotal</label>
          <%= text_field_tag "order[subtotal]", @order.subtotal, class: "form-control#{' error' unless @order.errors[:subtotal].blank?}", placeholder: 'Subtotal' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Tax</label>
          <%= text_field_tag "order[tax]", @order.tax, class: "form-control#{' error' unless @order.errors[:tax].blank?}", placeholder: 'Tax' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Discount</label>
          <%= text_field_tag "order[discount]", @order.discount, class: "form-control#{' error' unless @order.errors[:discount].blank?}", placeholder: 'Discount' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Shipping</label>
          <%= text_field_tag "order[shipping]", @order.shipping, class: "form-control#{' error' unless @order.errors[:shipping].blank?}", placeholder: 'Shipping' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Total</label>
          <%= text_field_tag "order[total]", @order.total, class: "form-control#{' error' unless @order.errors[:total].blank?}", placeholder: 'Total' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>

      <h3>Shipping Address</h3>
      <div class="form-group">
          <label>Street1</label>
          <%= text_field_tag "shipping_address[street1]", @order.shipping_address.street1, class: "form-control#{' error' unless @order.shipping_address.errors[:street1].blank?}", placeholder: 'Street1' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Street2</label>
          <%= text_field_tag "shipping_address[street2]", @order.shipping_address.street2, class: "form-control#{' error' unless @order.shipping_address.errors[:street2].blank?}", placeholder: 'Street2' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>City</label>
          <%= text_field_tag "shipping_address[city]", @order.shipping_address.city, class: "form-control#{' error' unless @order.shipping_address.errors[:city].blank?}", placeholder: 'City' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Region</label>
          <%= text_field_tag "shipping_address[region]", @order.shipping_address.region, class: "form-control#{' error' unless @order.shipping_address.errors[:region].blank?}", placeholder: 'Region' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Postal Code</label>
          <%= text_field_tag "shipping_address[region]", @order.shipping_address.region, class: "form-control#{' error' unless @order.shipping_address.errors[:region].blank?}", placeholder: 'Region' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Country</label>
          <%= select_tag "shipping_address[country]", options_for_select(country_options, @order.shipping_address.country), {class: "form-control#{' error' unless @order.shipping_address.errors[:country].blank?}"} %>
      </div>

      <h3>Billing Address</h3>
      <div class="form-group">
          <label>Street1</label>
          <%= text_field_tag "billing_address[street1]", @order.billing_address.street1, class: "form-control#{' error' unless @order.billing_address.errors[:street1].blank?}", placeholder: 'Street1' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Street2</label>
          <%= text_field_tag "billing_address[street2]", @order.billing_address.street2, class: "form-control#{' error' unless @order.billing_address.errors[:street2].blank?}", placeholder: 'Street2' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>City</label>
          <%= text_field_tag "billing_address[city]", @order.billing_address.city, class: "form-control#{' error' unless @order.billing_address.errors[:city].blank?}", placeholder: 'City' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Region</label>
          <%= text_field_tag "billing_address[region]", @order.billing_address.region, class: "form-control#{' error' unless @order.billing_address.errors[:region].blank?}", placeholder: 'Region' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Postal Code</label>
          <%= text_field_tag "billing_address[region]", @order.billing_address.region, class: "form-control#{' error' unless @order.billing_address.errors[:region].blank?}", placeholder: 'Region' %>
          <!--p class="help-block">Example block-level help text here.</p-->
      </div>
      <div class="form-group">
          <label>Country</label>
          <%= select_tag "billing_address[country]", options_for_select(country_options, @order.billing_address.country), {class: "form-control#{' error' unless @order.billing_address.errors[:country].blank?}"} %>
      </div>