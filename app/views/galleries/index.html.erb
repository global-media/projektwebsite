    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Gallery
            <%= link_to 'Add Gallery', new_admin_contents_gallery_url, class: "btn btn-primary pull-right" %>
            </h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    <div class="row">
        <div class="col-lg-6">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <!-- Hover Rows -->
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-hover sortable sorted_table">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Image URL</th>
                                    <th>&nbsp;</th>
                                </tr>
                            </thead>
                            <tbody>                              
                                <% @galleries.each do |gallery| %>
                                <tr class="sort" id="<%= gallery.id %>">
                                    <td><%= gallery.name %></td>
                                    <td><%= gallery.description %></td>
                                    <td><%= gallery.image_url %></td>
                                    <td>
                                      <%= link_to 'Edit', edit_admin_contents_gallery_url(id: gallery.id), class: "btn btn-warning" %>
                                      <%= link_to 'Delete', admin_contents_gallery_url(id: gallery.id), class: "btn btn-danger", method: :delete, data: { confirm: "Are you sure you want to delete this gallery?" } %>
                                    </td>
                                </tr>
                                <% end %>
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-6 -->
    </div>
    <!-- /.row -->
