    <%= render partial: 'tags/header', locals: {breadcrumbs: []} %>
    
    <%= form_tag  sort_admin_tags_url, {role: 'form', method: 'post'} do |f| %>
    
    <div class="row">
      <div class="col-md-offset-9 col-md-3">
        <%= link_to 'Add', new_admin_tag_url, alt: "Add", class: "btn btn-primary green" %>
        <%= button_tag 'Sort', type: 'submit', class: "btn btn-default" %>
      </div>
    </div>
    
    <!-- BEGIN PAGE CONTENT-->
		<div class="row">
			<div class="col-md-12">
				<!-- BEGIN SAMPLE TABLE PORTLET-->
				<div class="portlet">
					<div class="portlet-title">
						<div class="caption">
							<i class="fa fa-tags"></i>Tags
						</div>
						<div class="tools">
						  <a class="fullscreen" href="javascript:;" data-original-title="" title=""></a>
         			<a href="javascript:;" class="collapse"></a>
						</div>
					</div>
					<div class="portlet-body">
						<div class="table-scrollable">
							<table class="table table-striped table-bordered table-advance table-hover">
							<thead>
							<tr>
								<th class="col-md-1"><i class="fa fa-sort"></i> Sort</th>
								<th><i class="fa fa-info"></i> Name</th>
								<th><i class="fa fa-question"></i> Status</th>
								<th class="col-md-2">&nbsp;</th>
							</tr>
							</thead>
							<tbody>
							  <% @tags.each do |tag| %>
							  <%= hidden_field_tag "tag[sort][#{tag.id}][old]", tag.sort %>
							  <tr>
  								<td class="col-md-1"><%= text_field_tag "tag[sort][#{tag.id}][new]", tag.sort, class: 'form-control-size', placeholder: 'Sort #', size: 3, maxlength: 3 %></td>
                  <td><%= tag.name %></td>
                  <td><%= tag.status_name %>
                  <td class="col-md-2">
                    <%= link_to raw('<i class="icon-pencil"> Edit</i>'), edit_admin_tag_url(id: tag.id), class: "btn btn-warning btn-xs" %>
                    <%= link_to raw('<i class="icon-trash"> Delete</i>'), admin_tag_url(id: tag.id), class: "btn btn-danger btn-xs", method: :delete, data: { confirm: "Are you sure you want to delete this tag?" } %>
                  </td>
  							</tr>
							  <% end %>
							</tbody>
							</table>
						</div>
					</div>
				</div>
				<!-- END SAMPLE TABLE PORTLET-->
			</div>
		</div>
		<!-- END PAGE CONTENT-->
		<% end %>
